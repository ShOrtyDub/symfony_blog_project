{% extends 'base.html.twig' %}

{% block title %}Articles index{% endblock %}

{% block body %}

    <div>
        <h1>Profil index</h1>

        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Password</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.prenom }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.password }}</td>
                <td>
                    <a href="{{ path('app_account_profil_edit', {'id': user.id}) }}">edit</a>
                </td>
            </tr>

            </tbody>
        </table>

        <a href="{{ path('app_account') }}">back to home</a>
    </div>



{% endblock %}
