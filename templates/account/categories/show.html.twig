{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}

    <div>
        <h1>Categories</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.nom }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_account_categories_index') }}">back to list</a>
    </div>

{% endblock %}
