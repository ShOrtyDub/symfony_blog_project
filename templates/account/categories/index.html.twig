{% extends 'base.html.twig' %}

{% block title %}Blog Symfony | Categories{% endblock %}

{% block body %}

    <div>
        <h1>Categories index</h1>

        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
                <th>voir</th>
            </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr>
                    <td>{{ category.id }}</td>
                    <td>{{ category.nom }}</td>
                    <td>
                        <a href="{{ path('app_account_categories_show', {'id': category.id}) }}">show</a>
                    </td>
                    <td>
                        <a href="{{ path('app_account_articles_index', {'FK_categories': category.id}) }}">articles de la catégorie</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_account') }}">Back to home</a>

    </div>

{% endblock %}
